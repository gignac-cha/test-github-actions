name: test-workflow-command

on:
  push:
    branches:
      - tests/workflow-command

jobs:
  test-workflow-command:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::debug::Debug Message"
          echo "::notice file=file,line=line,endLine=endLine,title=title::Notice Message"
          echo "::warning file=file.js,line=123,endLine=456,title=title::Warning Message"
          echo "::error file=file.js,line=123,endLine=456,title=title::Error Message"
          exit 1
      - run: echo "Normal 1"
      - if: success()
        run: echo "Success 1"
      - run: echo "Normal 2"
      - if: failure()
        run: echo "Failure 1"
      - run: echo "Normal 3"
      - if: success()
        run: echo "Success 2"
      - run: echo "Normal 4"
      - if: failure()
        run: echo "Failure 2"
      - run: echo "Normal 5"
