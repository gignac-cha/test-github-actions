name: test-inner-script

on:
  pull_request:
    branches:
      - main

jobs:
  test-inner-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - uses: .github/actions/send-slack-notification
      #   with:
      #     slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
      #     test-web-url: ${{ vars.TEST_WEB_URL }}
      #     title: '✅ Unit Test Success'
      #     is-success: true
      # - uses: .github/actions/send-slack-notification
      #   with:
      #     slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
      #     test-web-url: ${{ vars.TEST_WEB_URL }}
      #     title: '⛔️ Integration Test Failed'
      #     is-success: false
      # - run: echo '${{ toJSON(github) }}'
      # - id: get-job-id
      #   run: |
      #     gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/attempts/${{ github.run_attempt }}/jobs | \
      #     jq '.jobs[] | select(.name == "${{ github.job }}") | .id' | \
      #     gawk '{ print "id=" $1 }' >> $GITHUB_OUTPUT
      #   env:
      #     GH_TOKEN: ${{ github.token }}
      # # - run: "echo Job ID: ${{ steps.get-job-id.outputs.id }}"
      # - id: test-script
      #   uses: actions/github-script@v7
      #   with:
      #     result-encoding: "json"
      #     script: |
      #       return { a: 1, b: 'test', c: false, d: [1,2,3] }
      # - run: |
      #     echo 1 '${{ steps.test-script.outputs.result }}'
      #     echo 2 '${{ steps.test-script.outputs.result }}' | jq
      #     echo 2.1 '${{ fromJSON(steps.test-script.outputs.result).a }}'
      #     echo 2.2 '${{ fromJSON(steps.test-script.outputs.result).b }}'
      #     echo 2.3 '${{ fromJSON(steps.test-script.outputs.result).c }}'
      #     echo 2.4 '${{ fromJSON(steps.test-script.outputs.result).d }}'
      #     echo 3 '${{ steps.test-script.outputs.result.a }}'
      #     echo 3.1 '${{ steps.test-script.outputs.result['a'] }}'
      #     echo 4 '${{ steps.test-script.outputs.result.b }}'
      #     echo 5 '${{ steps.test-script.outputs.result.c }}'
      #     echo 6 '${{ steps.test-script.outputs.result.d }}'
      - run: exit(1)
      # - id: test-script
      #   uses: actions/github-script@v7
      #   with:
      #     result-encoding: string
      #     script: |
      #       const createTextElement = ({ text, bold = false }) => ({ type: 'text', text, style: { bold } });
      #       return JSON.stringify({ blocks: [ createTextElement({ text: 'Test' }) ] });
      # - run: echo '${{ steps.test-script.outputs.result }}' | jq
      # - uses: ./.github/actions/test-action
      #   with:
      #     markdown: |
      #       First line
      #         second line
      #       third line
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: 22
      # - id: get-slack-id
      #   uses: ./.github/actions/get-slack-user-id
      #   with:
      #     user: ${{ github.event.pull_request.user.id }}
      # - id: generate-message
      #   run: |
      #     node -e 'console.log("message=" + `\
      #       ✅ *${{ 'name' }} Success* <@${{ steps.get-slack-id.outputs.id }}>\
      #       > *Pull Request*: ${{ github.event.pull_request.title }} <${{ github.event.pull_request.html_url }}|#${{ github.event.pull_request.number }}>\
      #       > *Commit*: <${{ github.event.pull_request.html_url }}/commits/${{ github.event.pull_request.head.sha }}|${"${{ github.event.pull_request.head.sha }}".slice(0, 8)}>\
      #       > *Build ID*: <${{ vars.TEST_WEB_URL }}|${{ github.run_number }}> (<${{ github.repository.html_url }}/actions/runs/${{ github.run_id }}|Workflow #${{ github.run_number }}>)\
      #     `)' >> $GITHUB_OUTPUT
      # - run: echo ${{ steps.generate-message.outputs.message }}

  after-job:
    runs-on: ubuntu-latest
    needs: test-inner-script
    if: always()
    steps:
      - run: echo Result = ${{ needs.test-inner-script.result }} !!!
      - if: needs.test-inner-script.result == 'success'
        run: echo SUCCESS!!
      - if: needs.test-inner-script.result == 'failure'
        run: echo FAILURE...
    
